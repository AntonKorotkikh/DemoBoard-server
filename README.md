## DemoBoard-server

DemoBoard-server is the backend API server component that talks to the DB and the frontend React server.

The project's tech stack: C#, ASP.NET Core, Entity Framework, Serilog, SQLite.
